<!--
    English: The Android package name (applicationId) for this app is
    "com.example.coffee_cart". This value is defined in
    android/app/build.gradle.kts -> defaultConfig -> applicationId.
    To change the package name, update the applicationId in that file and
    follow the Flutter docs for refactoring the Android package.

    Arabic: اسم الحزمة للتطبيق هو "com.example.coffee_cart". هذا المعرف محدد في
    android/app/build.gradle.kts في defaultConfig -> applicationId.
    لتغييره، عدّل applicationId في ذلك الملف واتبع خطوات Flutter لتغيير
    معرف الحزمة على Android.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.coffee_cart">
    <!-- English: Allow network access from the Android app so Appwrite HTTP/S requests can work. -->
    <!-- Arabic: السماح بالوصول إلى الشبكة من التطبيق حتى تعمل طلبات HTTP/S الخاصة بـ Appwrite. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:label="coffee_cart"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--  appwrite     -->    
          <activity
    android:name="io.appwrite.views.CallbackActivity"
    android:exported="true">
    <intent-filter>
        <action android:name="android.intent.action.VIEW" />

        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />

        <data
            android:scheme="appwrite-callback-68bf228300007baa47f9"
            android:host="oauth" />
    </intent-filter>
</activity>

    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest> 
